import * as bootstrap from 'bootstrap';

/**
 * Bootstrap diekspos melalui window agar komponen admin berbasis
 * vanilla JavaScript dapat mengakses Modal, Offcanvas, Dropdown,
 * Collapse, dan komponen Bootstrap lainnya.
 */
window.bootstrap = bootstrap;
